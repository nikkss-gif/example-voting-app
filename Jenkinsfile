pipeline {
    agent any

    stages {
        stage('✅ Test Connection') {
            steps {
                echo 'Success! Jenkins can read the Jenkinsfile from GitHub.'
            }
        }
    }
}
